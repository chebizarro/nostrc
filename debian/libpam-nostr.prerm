#!/bin/sh
set -e

case "$1" in
    remove|deconfigure)
        if command -v pam-auth-update >/dev/null 2>&1; then
            pam-auth-update --package --remove nostr
        fi
        ;;

    upgrade|failed-upgrade)
        ;;

    *)
        echo "prerm called with unknown argument \`$1'" >&2
        exit 1
        ;;
esac

#DEBHELPER#

exit 0
