# Test environment for local Docker Compose runs
# Copy this file to .env and fill in real values:
#   cp .env.example .env
#
# SIGNET_DB_KEY=<64-char hex master key for SQLCipher>
# SIGNET_BUNKER_NSEC=<nsec1... or 64-char hex secret key>
# SIGNET_RELAYS=wss://relay.example.com
